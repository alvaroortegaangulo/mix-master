# =====================================================
# PLANTILLA DE CONFIGURACIÓN - NO CONTIENE SECRETOS
# Copiar a .env y rellenar con valores reales
# =====================================================
#
# Para generar valores seguros, ejecuta:
#   SECRET_KEY: openssl rand -hex 32
#   PASSWORDS:  openssl rand -hex 16
#   TOKENS:     openssl rand -hex 16

# === Seguridad ===
# JWT signing key (64 caracteres hex)
SECRET_KEY=generate-with-openssl-rand-hex-32

# API token para autenticación interna
MIXMASTER_API_TOKEN=generate-with-openssl-rand-hex-16

# === Redis ===
# Contraseña de Redis (32 caracteres hex)
REDIS_PASSWORD=generate-with-openssl-rand-hex-16

# === PostgreSQL ===
POSTGRES_USER=mixmaster
POSTGRES_PASSWORD=generate-with-openssl-rand-hex-16
POSTGRES_DB=mixmaster
DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
DATABASE_SSL_MODE=disable

# === Pool de conexiones DB ===
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=12
DB_POOL_RECYCLE=900

# === Rate Limiting ===
MIXMASTER_RATE_LIMIT_REQUESTS=30
MIXMASTER_RATE_LIMIT_WINDOW_SECONDS=60
RATE_LIMIT_REDIS_URL=redis://default:${REDIS_PASSWORD}@redis:6379/0
RATE_LIMIT_REDIS_PREFIX=ratelimit:api:

# === CORS ===
# Lista separada por comas de orígenes permitidos
CORS_ALLOW_ORIGINS=https://music-mix-master.com,https://api.music-mix-master.com,https://www.music-mix-master.com

# === Google OAuth ===
# Client ID público de Google OAuth (obtener de Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# === Analytics (Frontend) ===
# Google Analytics Measurement ID
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# === Rutas y configuración ===
# Directorio temporal para archivos de procesamiento
MIXMASTER_TEMP_ROOT=/tmp

# === Logging ===
# Nivel de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
