version: "3.9"

services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "no"]
    networks:
      - app-net

  web:
    build: .
    command: uvicorn server:app --host 0.0.0.0 --port 8000
    environment:
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/0
      MIX_TEMP_ROOT: /dev/shm/mix-master/temp
      MPLCONFIGDIR: /tmp/mplconfig
      XDG_CACHE_HOME: /tmp/.cache        # para Fontconfig
    depends_on:
      - redis
    # Si necesitas exponer el backend directamente:
    ports:
      - "8000:8000"
    # Limitar superficie y permisos
    read_only: true
    tmpfs:
      - /dev/shm/mix-master/temp:size=2g
      - /tmp
    volumes:
      - web-temp:/app/temp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    restart: unless-stopped
    networks:
      - app-net

  # Ejemplo de worker (si lo tienes)
  worker:
    build: .
    command: celery -A celery_app worker --loglevel=info --concurrency=2
    environment:
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/0
      MPLCONFIGDIR: /tmp/mplconfig
      MIX_TEMP_ROOT: /dev/shm/mix-master/temp
      XDG_CACHE_HOME: /tmp/.cache
    depends_on:
      - redis
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      # Donde tú ya procesas audio (si lo usas):
      - /dev/shm/mix-master/temp:size=2g
      # /tmp writable para tempfile + Matplotlib
      - /tmp
    networks:
      - app-net   # importante, ver siguiente punto

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    command: npm run start -- -p 3000
    environment:
      NODE_ENV: production
      # Mejor usar el nombre del servicio interno en lugar de IP pública:
      NEXT_PUBLIC_BACKEND_URL: http://web:8000
    depends_on:
      - web
    # Mientras no tengas proxy, sigues exponiendo 3000. Idealmente luego lo pondrías tras nginx.
    ports:
      - "3000:3000"
    # Endurecer contenedor
    read_only: true
    tmpfs:
      - /tmp   # Next.js puede necesitar /tmp para cosas menores
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    restart: unless-stopped
    networks:
      - app-net

networks:
  app-net:

volumes:
  web-temp:
