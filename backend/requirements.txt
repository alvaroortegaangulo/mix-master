# Núcleo numérico / audio base
numpy
soundfile
scipy
matplotlib


# Análisis de audio
librosa
pyloudnorm
essentia

# Procesado de efectos (EQ, compresor, etc.)
pedalboard

# Backend web (API de mezcla)
fastapi
uvicorn[standard]
python-multipart   # necesario para UploadFile / multipart/form-data

# Cola de tareas (procesamiento asíncrono)
celery[redis]==5.2.7
redis

# Database
sqlalchemy
psycopg2-binary

# Auth
passlib[bcrypt]==1.7.4   # 1.7.4 compatible con bcrypt 3.x
bcrypt<4.0                # evitar incompatibilidad passlib vs bcrypt 4.x
python-jose[cryptography]
email-validator

# --- Opcionales históricos / futuros (comentados) ---
# psutil         # monitorización de memoria/CPU si lo quieres usar más adelante
# essentia       # análisis de audio avanzado si reactivas el pipeline antiguo
# pyrubberband   # time-stretch / pitch-shift de alta calidad (vía Rubber Band)
# pydantic       # FastAPI ya lo instala como dependencia; sólo añádelo si lo usas tú directamente
# ML / Deep Learning (lo puedes usar más adelante)
# torch
# torchvision
# torchaudio

aiofiles
google-auth
requests
